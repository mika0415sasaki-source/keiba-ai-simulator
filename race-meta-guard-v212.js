(function(){
  'use strict';
  if(window.__keibaRaceMetaGuardV212)return;
  window.__keibaRaceMetaGuardV212=true;

  const COURSE_MAP={'01':'札幌競馬場','02':'函館競馬場','03':'福島競馬場','04':'新潟競馬場','05':'東京競馬場','06':'中山競馬場','07':'中京競馬場','08':'京都競馬場','09':'阪神競馬場','10':'小倉競馬場'};
  const controls=['play','batch','navPlay','navBatch'];
  const resultIds=['ranking','analysis','aiSummary','valueSummary','trifectaPlan'];
  let token=0;

  const sleep=ms=>new Promise(r=>setTimeout(r,ms));
  function setButtons(v){controls.forEach(id=>{const e=document.getElementById(id);if(e)e.disabled=!!v;});}
  function blockResults(v){resultIds.forEach(id=>{const e=document.getElementById(id);if(!e)return;if(v){if(e.dataset.v212Display===undefined)e.dataset.v212Display=e.style.display||'';e.style.display='none';}else if(e.dataset.v212Display!==undefined){e.style.display=e.dataset.v212Display;delete e.dataset.v212Display;}});}
  function banner(text,state){let e=document.getElementById('raceMetaGuardV212')||document.getElementById('raceMetaGuardV211')||document.getElementById('raceMetaGuardV209');if(!e){e=document.createElement('div');e.id='raceMetaGuardV212';e.style.margin='8px 0';e.style.padding='10px 12px';e.style.borderRadius='8px';e.style.fontSize='12px';e.style.fontWeight='700';const t=document.getElementById('importStatus')||document.getElementById('jraEntryStatus');if(t&&t.parentNode)t.parentNode.insertBefore(e,t.nextSibling);}const m={pending:['#d29922','#2d2411'],ok:['#3fb950','#102a17'],error:['#f85149','#351313']};const c=m[state]||m.pending;e.style.color=c[0];e.style.background=c[1];e.style.border='1px solid '+c[0];e.textContent=text;}
  function inputUrl(){const e=document.getElementById('jraEntryUrl');return String(e&&e.value||'').trim();}
  function raceId(url){let s=String(url||'');try{s=decodeURIComponent(s);}catch(e){}const m=s.match(/[?&]race_id=(\d{12})/);return m?m[1]:'';}
  function inferVariant(course,surface,distance,dir){if(surface==='ダート')return '通常';const d=Number(distance),s=String(dir||'').normalize('NFKC');if(course==='新潟競馬場'){if(d===1000||/直線/.test(s))return '直線';if(/外/.test(s)||[1600,1800,3000,3200].includes(d))return '外回り';return '内回り';}if(course==='阪神競馬場'||course==='京都競馬場'||course==='中山競馬場')return /外/.test(s)?'外回り':'内回り';if(course==='小倉競馬場'){const m=s.match(/(?:^|\s)(A|B|C)(?:\s|$)/i);return m?m[1].toUpperCase():'A';}return '通常';}
  function parseMeta(text,course){const raw=String(text||'').normalize('NFKC').replace(/\r/g,'\n').replace(/(\d),(?=\d{3}\b)/g,'$1');const h=raw.slice(0,30000);let m=h.match(/(芝|ダート|ダ)\s*([1-4]\d{3})\s*(?:m|メートル)?\s*[（(]?\s*([^）)\n\/]{0,40})/i);let surface,distance,dir='';if(m){surface=m[1];distance=+m[2];dir=m[3]||'';}else{m=h.match(/([1-4]\d{3})\s*(?:m|メートル)?\s*[（(]?\s*(芝|ダート|ダ)\s*([^）)\n\/]{0,40})/i);if(!m)return null;distance=+m[1];surface=m[2];dir=m[3]||'';}if(surface==='ダ')surface='ダート';if(!distance||distance<1000||distance>4000)return null;return {course,surface,distance,variant:inferVariant(course,surface,distance,dir)};}
  function cacheKey(rid){return 'keiba_race_meta_v212_'+rid;}
  function loadCache(rid){for(const k of [cacheKey(rid),'keiba_race_meta_v211_'+rid,'keiba_race_meta_v209_'+rid,'keiba_race_meta_v208_'+rid,'keiba_race_meta_v206_'+rid]){try{const x=JSON.parse(localStorage.getItem(k)||'null');if(x&&x.course&&x.surface&&+x.distance)return x;}catch(e){}}return null;}
  function saveCache(rid,m){try{localStorage.setItem(cacheKey(rid),JSON.stringify(m));}catch(e){}}
  async function getMeta(url){const rid=raceId(url);if(!rid)throw new Error('race_idを確認できません');const course=COURSE_MAP[rid.slice(4,6)];if(!course)throw new Error('競馬場を確認できません');const cached=loadCache(rid);if(cached)return cached;const target='https://r.jina.ai/https://race.netkeiba.com/race/shutuba.html?race_id='+rid;const ac=new AbortController();const tm=setTimeout(()=>ac.abort(),6500);try{const r=await fetch(target,{cache:'no-store',signal:ac.signal});if(!r.ok)throw new Error('HTTP '+r.status);const m=parseMeta(await r.text(),course);if(!m)throw new Error('レース条件を読めません');saveCache(rid,m);return m;}finally{clearTimeout(tm);}}
  function findOption(el,value){const w=String(value);return Array.from(el&&el.options||[]).find(o=>String(o.value)===w||String(o.textContent)===w||String(o.textContent).replace(/m$/i,'')===w);}
  async function setSel(el,value,force){if(!el)return false;let o=findOption(el,value);if(!o&&force){o=document.createElement('option');o.value=String(value);o.textContent=String(value)+(el.id==='distance'?'m':'');el.appendChild(o);}if(!o)return false;el.value=o.value;el.dispatchEvent(new Event('change',{bubbles:true}));await sleep(30);return true;}
  async function apply(m){const c=document.getElementById('course'),s=document.getElementById('surface'),v=document.getElementById('variant'),d=document.getElementById('distance');if(!await setSel(c,m.course))throw new Error('競馬場');if(!await setSel(s,m.surface))throw new Error('芝/ダート');if(!await setSel(v,m.variant,true))throw new Error('コース区分');if(!await setSel(d,String(m.distance),true))throw new Error('距離');}
  function imported(){const s=String((document.getElementById('importStatus')||{}).textContent||'')+' '+String((document.getElementById('jraEntryStatus')||{}).textContent||'');return /\d+頭を?取り込みました|\d+頭を反映しました|直接反映しました/.test(s);}
  async function run(my,url){try{banner('条件確認中…','pending');const m=await getMeta(url);if(my!==token)return;await apply(m);for(let i=0;i<240&&my===token;i++){if(imported())break;await sleep(500);}if(my!==token)return;await apply(m);banner('✓ '+m.course.replace('競馬場','')+' / '+m.surface+' / '+m.variant+' / '+m.distance+'m','ok');blockResults(false);setButtons(false);}catch(e){if(my!==token)return;banner('⚠ 条件確認失敗: '+String(e&&e.message||e),'error');setButtons(true);blockResults(true);}}
  function mount(){const b=document.getElementById('fetchJraEntry');if(!b){setTimeout(mount,150);return;}if(b.dataset.v212)return;b.dataset.v212='1';b.addEventListener('click',()=>{const my=++token;setButtons(true);blockResults(true);run(my,inputUrl());},true);}
  mount();
})();
